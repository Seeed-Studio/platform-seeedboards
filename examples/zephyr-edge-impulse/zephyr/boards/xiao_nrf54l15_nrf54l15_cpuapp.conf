# Edge Impulse XIAO variant: do not use stream flash / external layout forcing
# CONFIG_STREAM_FLASH=n
# CONFIG_STREAM_FLASH_INSPECT=n
# If still selected indirectly, force off page layout (not needed without stream flash usage)
# CONFIG_FLASH_PAGE_LAYOUT=n